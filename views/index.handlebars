    <div>
        <button id = "savedArticles">See Saved Articles Only and Comment on them</button>
    <button id= "scrape">Go and Get some more Reddit</button>
    <button id= "home">Home</button>
    <div id= "articles"></div>
{{#each responses}}
     <div id= {{this.id}}>
     <p class = "title{{this.id}}">{{this.title}}<br /></p>
     <p class= "link{{this.id}}">{{this.link}}</p>
     <button positionid= {{this.id}} class = 'saveArt'>Save</button>
     </div>
    {{/each}}
    {{!-- <div id ="myModal" class= "modal">
    <div class= "modal-content">
        <span class="close">&times;</span>
        <div id="notes"></div> --}}
    </div>
    </div>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="../js/app.js"></script>
{{!-- NAV PARTIAL --}}
<div class="nav">
  {{>navBar}}
</div>
<br><br>
       
    <div id= "articles">
       
    </div>
    <div class="jumbotron">
       <h1>Welcome to the Reddit Web Developer Scrapper</h1>
    </div>
    
<div class="row">{{#if responses}}
{{#each responses}}
  <div class="col-sm-4">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"></h5>
        <p class="card-text-center title{{this.id}}">{{this.title}}</p>
        <a positionid= {{this.id}} class="btn btn-primary saveArt">Save it</a>
        <a target="_blank" class= "btn btn-primary link{{this.id}}" href={{detectReddit this.link}}>Check out the page</a>
      </div>
    </div>
  </div>

    {{/each}}
    {{else}}
    

    </div>
    <h1 class= "text-center">Go Scrape something</h1>
        {{/if}}
    </div>
    </div>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="../js/app.js"></script>